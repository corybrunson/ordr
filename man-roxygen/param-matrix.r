#' @param .matrix A character string partially matched (lowercase) to several
#'   indicators for one or both matrices in a matrix decomposition used for
#'   ordination. The standard values are `"rows"`, `"cols"`, and `"dims"` (for
#'   both).
